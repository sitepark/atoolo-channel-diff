services:
    _defaults:
        autowire: true
        autoconfigure: true

    _instanceof:
        Symfony\Component\Console\Command\Command:
            tags: ["command"]

    Atoolo\ChannelDiff\:
        resource: "../src/"
        exclude:
            - "../src/Kernel.php"
